/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['nftstorage.link'], // Add 'gateway.ipfs.io' to the list
      },
    // ... any other existing configurations
  }
  
  module.exports = nextConfig; 
  